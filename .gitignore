# Local operations evidence and release dry-run artifacts.
ops-evidence/

# Stray tool caches at the repo root (real dependencies live in frontend/node_modules).
node_modules/

# Subagent-driven development scratch (ledger, briefs, review packages).
.superpowers/

# Scrapling extractor sidecar's own Python virtualenv.
services/scrapling-extractor/.venv/

# Scrapling extractor adaptive-selector store when run via run-local.sh.
services/scrapling-extractor/.data/

# Python bytecode and pytest caches (crawler, extractor, fixture scripts).
__pycache__/
.pytest_cache/
